// NOTE: Backend API disabled in demo.
// All workout data is stored locally in browser storage only.



//const BASE_URL = "http://127.0.0.1:8000";

// Fetch all workouts
export const getWorkouts = async () => {
  
};

// Add a new workout
export const addWorkout = async (workout) => {
};